import React from "react";
import './cart.scss';
import CartItemsList from "../cart-items-list/cart-items-list";

const Cart = () => {
    return (
        <div>
            <CartItemsList/>
        </div>
    )
};

export default Cart;